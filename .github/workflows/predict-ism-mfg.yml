name: Predict ISM Manufacturing PMI

on:
  # Daily at 14:25 UTC — offset from NFP/CPI (14:00), FOMC (14:05),
  # PPI (14:10), PCE (14:15), Retail (14:20).
  schedule:
    - cron: '25 14 * * *'
    - cron: '0 12 * * *'
  workflow_dispatch:
    inputs:
      force_release_date:
        description: 'Override ISM Mfg release date (YYYY-MM-DD).'
        required: false
      force_days_out:
        description: 'Override days_out (1|2|3|4|7).'
        required: false

concurrency:
  group: predict-ism-mfg
  cancel-in-progress: false

jobs:
  gate:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
      ism_mfg_release_date: ${{ steps.check.outputs.ism_mfg_release_date }}
      ism_mfg_days_out: ${{ steps.check.outputs.ism_mfg_days_out }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version-file: '.python-version'
      - name: Decide if today is an ISM Mfg predict day
        id: check
        run: |
          if [ -n "${{ github.event.inputs.force_release_date }}" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "ism_mfg_release_date=${{ github.event.inputs.force_release_date }}" >> "$GITHUB_OUTPUT"
            echo "ism_mfg_days_out=${{ github.event.inputs.force_days_out }}" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if python scripts/should_run_ism_mfg.py; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          fi

  predict:
    needs: gate
    if: needs.gate.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-python@v5
        with:
          python-version-file: '.python-version'
          cache: 'pip'
      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch live ISM Mfg consensus + last-known anchor from worker
        id: consensus
        env:
          CALENDAR_AUTH_KEY: ${{ secrets.CALENDAR_AUTH_KEY }}
          ISM_MFG_RELEASE_DATE: ${{ needs.gate.outputs.ism_mfg_release_date }}
        run: |
          python - <<'PY'
          import json, os, re, sys, urllib.parse, urllib.request
          key = os.environ["CALENDAR_AUTH_KEY"]
          rel = os.environ["ISM_MFG_RELEASE_DATE"]
          url = ("https://faractionradar-calendar.faractionradar.workers.dev/"
                 f"?key={urllib.parse.quote(key)}&read")
          req = urllib.request.Request(url, headers={
              "user-agent": "Mozilla/5.0 (X11; Linux x86_64; rv:129.0) Gecko/20100101 Firefox/129.0",
          })
          data = json.loads(urllib.request.urlopen(req, timeout=30).read())
          events = data.get("events", [])

          def is_ism_mfg(title: str) -> bool:
              t = (title or "").strip().lower()
              return bool(re.match(r"^ism\s+manufacturing\s+pmi$", t))

          matches = [
              e for e in events
              if e.get("country") == "USD"
              and is_ism_mfg(e.get("title") or "")
              and (e.get("date") or "").startswith(rel)
          ]

          if not matches:
              print(f"::notice::no ISM Manufacturing PMI in worker /?read for {rel}")
              sys.exit(0)
          ev = matches[0]

          def parse_index(s):
              if not s: return None
              m = re.match(r"^([-+]?\d+(?:\.\d+)?)$", s.strip())
              return float(m.group(1)) if m else None

          consensus = parse_index(ev.get("forecast"))
          anchor = parse_index(ev.get("previous"))
          with open(os.environ["GITHUB_OUTPUT"], "a") as f:
              if consensus is not None:
                  f.write(f"consensus={consensus}\n")
              if anchor is not None:
                  f.write(f"anchor={anchor}\n")
          print(f"consensus={consensus} anchor={anchor}")
          PY

      - name: Run ISM Mfg predictor + POST to calendar-worker
        env:
          UPLOAD_AUTH_KEY: ${{ secrets.UPLOAD_AUTH_KEY }}
          CALENDAR_WORKER_URL: https://faractionradar-calendar.faractionradar.workers.dev
          ISM_MFG_RELEASE_DATE: ${{ needs.gate.outputs.ism_mfg_release_date }}
          ISM_MFG_DAYS_OUT: ${{ needs.gate.outputs.ism_mfg_days_out }}
          ISM_MFG_CONSENSUS: ${{ steps.consensus.outputs.consensus }}
          ISM_MFG_ANCHOR: ${{ steps.consensus.outputs.anchor }}
          MODEL_VERSION: v1-simple-blend
        run: python emit_ism_mfg.py

      - name: Commit report + ledger row
        run: |
          git config user.name  "faractionradar-bot"
          git config user.email "bot@faractionradar.com"
          git add reports/ predictions.jsonl || true
          if git diff --cached --quiet; then echo "nothing to commit"; exit 0; fi
          git commit -m "ISM Mfg prediction T-${{ needs.gate.outputs.ism_mfg_days_out }} for ${{ needs.gate.outputs.ism_mfg_release_date }}

          Auto-generated by predict-ism-mfg workflow. v1-simple-blend."
          for i in 1 2 3 4 5; do
            if git push 2>&1; then exit 0; fi
            echo "push attempt $i failed, rebasing + retrying..."
            git pull --rebase --autostash 2>&1 || true
            sleep 3
          done
          echo "push failed after 5 attempts" >&2
          exit 1
