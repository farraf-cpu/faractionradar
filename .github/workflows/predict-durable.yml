name: Predict Durable Goods Orders

on:
  # Daily at 14:55 UTC (next offset after ADP 14:50).
  schedule:
    - cron: '55 14 * * *'
  workflow_dispatch:
    inputs:
      force_release_date:
        description: 'Override Durable release date (YYYY-MM-DD).'
        required: false
      force_days_out:
        description: 'Override days_out (1|2|3|4|7).'
        required: false

concurrency:
  group: predict-durable
  cancel-in-progress: false

jobs:
  gate:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
      durable_release_date: ${{ steps.check.outputs.durable_release_date }}
      durable_days_out: ${{ steps.check.outputs.durable_days_out }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version-file: '.python-version'
      - name: Decide if today is a Durable predict day
        id: check
        run: |
          if [ -n "${{ github.event.inputs.force_release_date }}" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "durable_release_date=${{ github.event.inputs.force_release_date }}" >> "$GITHUB_OUTPUT"
            echo "durable_days_out=${{ github.event.inputs.force_days_out }}" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if python scripts/should_run_durable.py; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          fi

  predict:
    needs: gate
    if: needs.gate.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-python@v5
        with:
          python-version-file: '.python-version'
          cache: 'pip'
      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch live Durable consensus from calendar-worker
        id: consensus
        env:
          CALENDAR_AUTH_KEY: ${{ secrets.CALENDAR_AUTH_KEY }}
          DURABLE_RELEASE_DATE: ${{ needs.gate.outputs.durable_release_date }}
        run: |
          python - <<'PY'
          import json, os, re, sys, urllib.parse, urllib.request
          key = os.environ["CALENDAR_AUTH_KEY"]
          rel = os.environ["DURABLE_RELEASE_DATE"]
          url = ("https://faractionradar-calendar.faractionradar.workers.dev/"
                 f"?key={urllib.parse.quote(key)}&read")
          req = urllib.request.Request(url, headers={
              "user-agent": "Mozilla/5.0 (X11; Linux x86_64; rv:129.0) Gecko/20100101 Firefox/129.0",
          })
          data = json.loads(urllib.request.urlopen(req, timeout=30).read())
          events = data.get("events", [])

          def is_durable(title: str) -> bool:
              t = (title or "").strip().lower()
              # Match "Durable Goods Orders m/m" (headline). Skip "Core Durable
              # Goods Orders m/m" — separate Phase 2 slug.
              return bool(re.match(r"^durable\s+goods\s+orders\s+m\/?m$", t))

          matches = [
              e for e in events
              if e.get("country") == "USD"
              and is_durable(e.get("title") or "")
              and (e.get("date") or "").startswith(rel)
          ]

          if not matches:
              print(f"::notice::no Durable Goods in worker /?read for {rel}")
              sys.exit(0)
          forecast = matches[0].get("forecast")
          if not forecast:
              print(f"::notice::Durable event present but forecast null for {rel}")
              sys.exit(0)
          m = re.match(r"^([-+]?\d+(?:\.\d+)?)\s*%?$", forecast.strip())
          if not m:
              print(f"::error::unparseable Durable forecast: {forecast!r}")
              sys.exit(1)
          consensus_pct = float(m.group(1))
          with open(os.environ["GITHUB_OUTPUT"], "a") as f:
              f.write(f"consensus_pct={consensus_pct}\n")
          print(f"consensus for Durable {rel}: {consensus_pct}%")
          PY

      - name: Run Durable predictor + POST to calendar-worker
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          UPLOAD_AUTH_KEY: ${{ secrets.UPLOAD_AUTH_KEY }}
          CALENDAR_WORKER_URL: https://faractionradar-calendar.faractionradar.workers.dev
          DURABLE_RELEASE_DATE: ${{ needs.gate.outputs.durable_release_date }}
          DURABLE_DAYS_OUT: ${{ needs.gate.outputs.durable_days_out }}
          DURABLE_CONSENSUS_PCT: ${{ steps.consensus.outputs.consensus_pct }}
          MODEL_VERSION: v1-simple-blend
        run: python emit_durable.py

      - name: Commit report + ledger row
        run: |
          git config user.name  "faractionradar-bot"
          git config user.email "bot@faractionradar.com"
          git add reports/ predictions.jsonl || true
          if git diff --cached --quiet; then echo "nothing to commit"; exit 0; fi
          git commit -m "Durable prediction T-${{ needs.gate.outputs.durable_days_out }} for ${{ needs.gate.outputs.durable_release_date }}

          Auto-generated by predict-durable workflow. v1-simple-blend."
          for i in 1 2 3 4 5; do
            if git push 2>&1; then exit 0; fi
            echo "push attempt $i failed, rebasing + retrying..."
            git pull --rebase --autostash 2>&1 || true
            sleep 3
          done
          echo "push failed after 5 attempts" >&2
          exit 1
